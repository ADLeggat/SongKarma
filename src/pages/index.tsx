import { NextPage } from "next";
import Layout from "../components/Layout";

const Home: NextPage = () => {
    return (
        <div>
            <Layout path="/" title="Home">
                SIGN UP PAGE
            </Layout>
        </div>
    );
};

export default Home;
